<Page
    x:Class="NexusStrap.UI.Views.Pages.PluginsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <ScrollViewer Style="{StaticResource PageScrollViewer}" Padding="24">
        <StackPanel>
            <Grid Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Plugins" FontSize="28" FontWeight="Bold" />
                    <TextBlock Text="{Binding StatusText}" Opacity="0.6" Margin="0,4,0,0" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <ui:Button
                        Content="Refresh"
                        Icon="{ui:SymbolIcon ArrowClockwise24}"
                        Appearance="Secondary"
                        Command="{Binding RefreshAllCommand}"
                        Margin="0,0,8,0" />
                    <ui:Button
                        Content="Unload all"
                        Icon="{ui:SymbolIcon PlugDisconnected24}"
                        Appearance="Secondary"
                        Command="{Binding UnloadAllCommand}"
                        Margin="0,0,8,0" />
                    <ui:Button
                        Content="Open folder"
                        Icon="{ui:SymbolIcon Folder24}"
                        Appearance="Primary"
                        Command="{Binding OpenPluginsFolderCommand}" />
                </StackPanel>
            </Grid>

            <Border Background="{DynamicResource ThemeSurfaceBrush}" CornerRadius="8" Padding="16">
                <StackPanel>
                    <TextBlock Text="Plugins" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                    <ItemsControl ItemsSource="{Binding Plugins}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border
                                    BorderBrush="#33FFFFFF"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="12"
                                    Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock FontWeight="SemiBold" Text="{Binding Id}" />
                                            <TextBlock Margin="0,4,0,0" Opacity="0.75">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="Not loaded" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                                                <Setter Property="Text" Value="Loaded" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                        <StackPanel
                                            Grid.Column="1"
                                            Margin="16,0,0,0"
                                            VerticalAlignment="Center"
                                            Orientation="Horizontal">
                                            <ui:Button
                                                Content="Load"
                                                Margin="0,0,8,0"
                                                Command="{Binding DataContext.LoadPluginCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}">
                                                <ui:Button.Style>
                                                    <Style TargetType="ui:Button">
                                                        <Setter Property="IsEnabled" Value="True" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                                                <Setter Property="IsEnabled" Value="False" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ui:Button.Style>
                                            </ui:Button>
                                            <ui:Button
                                                Content="Unload"
                                                Appearance="Secondary"
                                                Command="{Binding DataContext.UnloadPluginCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}">
                                                <ui:Button.Style>
                                                    <Style TargetType="ui:Button">
                                                        <Setter Property="IsEnabled" Value="False" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                                                <Setter Property="IsEnabled" Value="True" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ui:Button.Style>
                                            </ui:Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
