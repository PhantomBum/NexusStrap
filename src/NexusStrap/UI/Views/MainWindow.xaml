<ui:FluentWindow
    x:Class="NexusStrap.UI.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:pages="clr-namespace:NexusStrap.UI.Views.Pages"
    mc:Ignorable="d"
    Title="NexusStrap"
    Width="1280" Height="800"
    MinWidth="960" MinHeight="600"
    WindowStartupLocation="CenterScreen"
    ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="NexusStrap" ShowMaximize="True" ShowMinimize="True" />

        <ui:NavigationView
            x:Name="NavigationView"
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleVisible="True"
            OpenPaneLength="220">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="Dashboard" Icon="{ui:SymbolIcon Home24}" TargetPageType="{x:Type pages:DashboardPage}" />
                <ui:NavigationViewItem Content="Performance" Icon="{ui:SymbolIcon TopSpeed24}" TargetPageType="{x:Type pages:PerformancePage}" />
                <ui:NavigationViewItem Content="Servers" Icon="{ui:SymbolIcon Globe24}" TargetPageType="{x:Type pages:ServerPage}" />
                <ui:NavigationViewItem Content="FastFlags" Icon="{ui:SymbolIcon Flag24}" TargetPageType="{x:Type pages:FastFlagPage}" />
                <ui:NavigationViewItem Content="Mods" Icon="{ui:SymbolIcon Puzzle24}" TargetPageType="{x:Type pages:ModsPage}" />
                <ui:NavigationViewItem Content="Plugins" Icon="{ui:SymbolIcon PlugConnected24}" TargetPageType="{x:Type pages:PluginsPage}" />
                <ui:NavigationViewItem Content="Macros" Icon="{ui:SymbolIcon Keyboard24}" TargetPageType="{x:Type pages:MacrosPage}" />
                <ui:NavigationViewItem Content="Customize" Icon="{ui:SymbolIcon PaintBrush24}" TargetPageType="{x:Type pages:CustomizationPage}" />
                <ui:NavigationViewItem Content="Monitor" Icon="{ui:SymbolIcon DataUsage24}" TargetPageType="{x:Type pages:MonitorPage}" />
                <ui:NavigationViewItem Content="Utilities" Icon="{ui:SymbolIcon Wrench24}" TargetPageType="{x:Type pages:UtilitiesPage}" />
                <ui:NavigationViewItem Content="Stability" Icon="{ui:SymbolIcon ShieldCheckmark24}" TargetPageType="{x:Type pages:StabilityPage}" />
                <ui:NavigationViewItem Content="Social" Icon="{ui:SymbolIcon People24}" TargetPageType="{x:Type pages:SocialPage}" />
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="Settings" Icon="{ui:SymbolIcon Settings24}" TargetPageType="{x:Type pages:SettingsPage}" />
            </ui:NavigationView.FooterMenuItems>

        </ui:NavigationView>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{DynamicResource ThemeSurfaceBrush}" Padding="12,4">
            <Grid>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Opacity="0.7" FontSize="11" />
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding RobloxVersion}" Margin="0,0,16,0" Opacity="0.5" FontSize="11" />
                    <TextBlock Text="NexusStrap v1.0.0" Opacity="0.5" FontSize="11" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
